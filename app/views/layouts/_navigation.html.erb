  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#"><%= link_to "virtinotes on the Fridge", root_path, id: "logo" %></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "midnight snackers", users_path, class: "nav-link"%>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            virtinote
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if logged_in? %>
              <%= link_to "add a virtinote", new_article_path, class: "dropdown-item" %>
            <% end %>
            <%= link_to "see virtinotes", articles_path, class: "dropdown-item" %>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="#">future feature</a></li>
          </ul>
        </li>

        <% if logged_in? %>              
      
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              hey <%= current_user.username %> <%= " -admin" if current_user.admin? %>
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to "view profile", user_path(current_user), class: "dropdown-item" %>
              <%= link_to "edit profile", edit_user_path(current_user), class: "dropdown-item" %>
              <li><hr class="dropdown-divider"></li>
              <%= link_to "delete profile and virtinotes", user_path(current_user), class: "dropdown-item text-danger", method: :delete, data: {confirm: "you sure?"} %>
            </ul>
          </li>      
          <li class="nav-item">
            <%= link_to 'go to bed', logout_path, class: "nav-link", method: :delete %>
          </li>

        <% else %>
          <li class="nav-item">
            <%= link_to 'come on in', login_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'sign me up', signup_path, class: "nav-link" %>
          </li>
        <% end %>

      </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </nav>